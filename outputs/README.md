# Outputs

`generated/` is the default destination for files created by the analysis
script. It is excluded from Git because it includes cleaned opportunity-level
samples and influence diagnostics.

`public/` contains only non-confidential aggregate material already reported in
the dissertation. Before adding any further file to `public/`, confirm that it
contains no project identifiers, row-level records, small-cell disclosures or
internal commercial information.
