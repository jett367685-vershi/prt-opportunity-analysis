# Suggested dissertation appendix text

## Code and reproducibility

The Python code used for the empirical analysis is available at
`REPLACE_WITH_GITHUB_REPOSITORY_URL` (release `v1.0.0`; commit
`REPLACE_WITH_COMMIT_HASH`). The repository contains the analysis script,
package requirements, a data dictionary and the non-confidential aggregate
figure reported in the dissertation.

The source workbook is not included because it contains commercially
confidential opportunity-level information. Consequently, the public
repository permits inspection of the analytical procedures, but exact
reproduction of the numerical results requires authorised access to a
workbook with the structure described in the repository data dictionary.

## Placement note

This statement can appear in an appendix on code and reproducibility. A
Generative AI Use Statement should remain a separate appendix if required by
the programme guidance. Replace the repository URL and commit hash only after
the final version has been uploaded and released.
